# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Product context

This repo is the **v2.0.0 LGO frontend** for Blockpot's own Licensed Gaming Operator. It connects wallet-connected players to neutral, immutable lottery protocol contracts deployed separately (see `../unipot-contracts`). Conceptually this frontend is a licensed lottery operator, **not** the protocol itself — future third-party LGOs may deploy their own frontends against the same protocol.

The v2 contracts strip BPT, governance, staking, referrals, contributor rewards, and start-draw bounties. Entries are gated by an on-chain `ComplianceRegistry` that whitelists operator addresses. See `V2_REFACTOR_INSTRUCTIONS.md` for full background.

## Commands

### Development
- `bun dev` — Start Vite dev server on port 4000
- `bun run build` — Vite production build (`bun build` is the Bun bundler, not the project build — use `bun run build`)
- `bun typecheck` — `tsc --noEmit`
- `bun lint` — ESLint with `--max-warnings 0`
- `bun run test` — Run vitest in `happy-dom` (use `bun run test`, not `bun test` — the latter invokes Bun's built-in runner)
- `bun run test:watch` — Vitest in watch mode
- `bun storybook` — Start Storybook on port 6006

### Contract sync
- `bun sync-abis` — Runs `./sync-abis.sh ../unipot-contracts src/abi`. Requires the contracts repo checked out alongside, with `forge` and `cargo` available. Re-run after any contract change.

## Architecture overview

### Tech stack
- **Build / framework**: Vite 7 + React 19 with TanStack Router (SPA, file-based routing under `src/routes/`)
- **Data layer**: TanStack Query v5 with IndexedDB persistence
- **Web3**: wagmi 3 + viem 2
- **Styling**: TailwindCSS v4 (CSS-based `@theme`, no `tailwind.config.js`) + `@blockpot-dev/blockpot-design-system` + local Radix/shadcn primitives under `src/components/ui/`

### Routing
- File-based, under `src/routes/`
- Routes: `/play` (default), `/transparency`, `/how-to-play`
- `src/routeTree.gen.ts` is auto-generated by `@tanstack/router-plugin/vite` on dev/build — do not hand-edit
- All route components lazy-load

### Directory structure
- `/src/components/blockpot/` — feature components (entries, current-round, drawn-numbers-panel, round-draw, info-panel, previous-rounds, play, transparency, header, navigation, modals, common)
- `/src/components/core/` — layout primitives (HStack, VStack, Container, Token, ContainerHeading, AnimatingNumber)
- `/src/components/ui/` — shadcn-style Radix wrappers
- `/src/components/web3/` — web3 UI (wallet options dialog)
- `/src/hooks/contracts/` — per-contract read/write hooks (`lottery/`, `compliance-registry/`, `chainlink/`, `erc20/`, `weth/`, `transparency/`, plus `read/` factories)
- `/src/hooks/entry/`, `/hooks/forms/`, `/hooks/utilities/`, `/hooks/web3/` — orchestration and utility hooks
- `/src/providers/` — context providers
- `/src/utilities/` — formatters, decimals, time, lottery helpers
- `/src/types/` — TypeScript type definitions (`lottery/`, `web3/`, `ui/`)
- `/src/abi/` — contract ABIs, synced from the contracts repo
- `/src/constants/` — contract addresses, protocol constants, operator env validation

### v2 contract surface

`ContractName` enum (`src/constants/contract-addresses.ts`): `LOTTERY_MAIN`, `CHAINLINK_AGGREGATOR_V3`, `FUNDS_MANAGER_MAIN`, `COMPLIANCE_REGISTRY`, `QUICK_GAME`, `WETH`. Per-chain addresses in `src/constants/contract-addresses/{blockpot-testnet,arbitrum-testnet,polygon-testnet,local}.ts`.

ABIs in `src/abi/`: `lotteryAbi`, `fundsManagerAbi`, `complianceRegistryAbi`, `wethAbi`.

### Environment variables
Vite-prefixed, loaded from `.env.local`:
- `VITE_APP_MODE` — `STAGING` (Blockpot testnet + mainnet) or anything else (Hardhat local)
- `VITE_WALLETCONNECT_PROJECT_ID`
- `VITE_OPERATOR_ADDRESS` — LGO operator wallet. Must be whitelisted in `ComplianceRegistry`. Validated at startup; if invalid, `Web3Provider` renders a full-page config error and refuses to mount the app.
- `VITE_OPERATOR_FEE_BPS` — operator fee in basis points (500 = 5%). Defaults to 500. Clamped to ≤ 2000.
- `VITE_MOCK_COUNTRY` — `TRUE` to bypass country detection in dev.

## Code style

### ESLint
- No semicolons, single quotes, 4-space indentation
- `unused-imports/no-unused-imports: error`
- Config: `eslint.config.mjs` (flat config, ESLint 9+)

### TypeScript
- Strict mode
- Contract-derived types via `abitype`'s `ExtractAbiFunction` / `AbiParametersToPrimitiveTypes`
- Bigint for token amounts, convert to string for display via viem's `formatEther`/`parseEther`

### Component patterns
- Functional components with hooks
- Export props type with `Props` suffix
- Prefer design system components first, then `src/components/ui/`, then `src/components/core/` primitives
- `HStack`/`VStack` from `@/components/core/`, **not** `@/components/design-system/`

### Hook patterns
```typescript
export default function useMyHook() {
    const chainId = useChainId()
    const { data, isLoading } = useQuery({
        queryKey: ['uniqueKey', chainId],
        queryFn: async () => { /* ... */ }
    })
    return { data, isLoading }
}
```

Organization:
- `/src/hooks/contracts/<contract-name>/` for per-contract domain hooks
- `/src/hooks/contracts/read/use*Read.ts` for generic read factories wrapping `useReadContract`
- `/src/hooks/web3/useTrackedContractWrite.ts` for all writes — transactions flow through `TransactionsProvider` automatically

## Entry flow (v2)

Critical to understand: the frontend collects `LEF = PEA + CF + OF` as two on-chain transactions because the protocol is operator-agnostic.

- **PEA** (Protocol Entry Amount): `0.001 ETH` per entry, hardcoded. Flows to the prize pool.
- **CF** (Contributor Fee): `2%` of PEA. Routed by the Lottery contract to a hardcoded DevCo address.
- **OF** (Operator Fee): `5%` of PEA by default (`VITE_OPERATOR_FEE_BPS`). Invisible to the protocol — this frontend transfers it directly from the user's wallet to `VITE_OPERATOR_ADDRESS`.

`useEnterLottery` (`src/hooks/contracts/lottery/actions/useEnterLottery.ts`) implements the two-step flow:

1. `sendTransactionAsync({ to: OPERATOR_ADDRESS, value: of })` — pay OF.
2. On success, `enter([roundIndex, amount, payoutInWeth, OPERATOR_ADDRESS], { value: pea + cf })` — submit the lottery entry.

Both hook calls also gate on `useIsOperatorWhitelisted`. If the operator is not whitelisted in `ComplianceRegistry`, `enter` rejects without spending any gas, and the `OperatorStatusBanner` surfaces a blocking message site-wide.

**Known limitation**: if step 1 succeeds and step 2 fails, the user has paid OF without receiving tickets. This is a refund-required state, tracked in `TODO.md`. A thin LGO router contract (Option B) is the planned follow-up.

Cost-breakdown constants live in `src/constants/protocol.ts` (`PEA_PER_ENTRY_WEI`, `CF_BASIS_POINTS`, `BASIS_POINTS_DIVISOR`) and `src/constants/operator.ts` (`OPERATOR_FEE_BPS`, `OPERATOR_ADDRESS`).

## Web3 integration

### Contract reads
```typescript
import useReadContract from '@/hooks/contracts/read/useReadContract'
import { lotteryAbi } from '@/abi/lotteryAbi'
import { ContractName } from '@/constants/contract-addresses'

// Returns a viem contract object with .read methods
const game = useReadContract(ContractName.LOTTERY_MAIN, lotteryAbi).read
const pots = await game.currentPots()
```

Each contract also has a convenience factory (`useLotteryRead`, `useComplianceRegistryRead`, etc.) under `src/hooks/contracts/read/`.

### Contract writes
```typescript
import useTrackedContractWrite from '@/hooks/web3/useTrackedContractWrite'

const { writeAsync, status, isLoading } = useTrackedContractWrite({
    address,
    abi: lotteryAbi,
    functionName: 'enter'
})

await writeAsync([args...], 'Notification title', { value: pea + cf })
```

Status flows `userPrompt → pending → success | reverted | cancelled`, with Sonner toasts. History persists to `localStorage` keyed by chainId.

## Data fetching

- TanStack Query v5 with IndexedDB persistence (idb-keyval, buster `v4`)
- Default stale/gc: 1h; Chainlink price feed: 60s
- Query-key convention: `['<domain>', selectedGame?, chainId, userAddress?, ...]`
- Event-driven refetch via `useEventsBlockNumber` — include the returned block in the query key, and contract emits trigger invalidation through `BlockpotEventsProvider`

## Provider stack (`src/routes/__root.tsx`)

Outer → inner:
1. CountryProvider
2. QueryClientProvider
3. Web3Provider (short-circuits to config-error page if `VITE_OPERATOR_ADDRESS` is invalid)
4. Web3ConnectionProvider
5. TransactionsProvider
6. SelectedGameProvider
7. ModalOpenStateProvider
8. BlockpotEventsProvider
9. BlockpotProvider
10. MissedDrawProvider
11. LotteryProvider (round pagination only)
12. BlockpotDrawProvider

Below `<Header/>`: `<OperatorStatusBanner/>` — renders a blocking banner if the configured operator is not whitelisted, nothing otherwise.

## Data formatting

- `formatEtherMaxDecimalsGreedy(value: bigint, maxDecimals: number)`
- `formatUSDCurrency(value, maxFractionDigits, leadingSign?)`
- `useFiatConverter` — hook-based ETH → USD conversion via Chainlink feed

Prefer `useFormattedCurrencyValues` when you need both native + fiat in one call.

## Key design system components

From `@blockpot-dev/blockpot-design-system`:
- `Button`, `Container`, `Input`, `Checkbox`, `SegmentedControl`, `Vortex`, `Dialog*`, `InfoField`, `SocialButton`, `ElevatedIcon`

Layout (local primitives in `@/components/core/`):
- `HStack`, `VStack`, `Container`

## Testing

- `bun run test` runs `vitest` against `src/**/*.test.{ts,tsx}` in `happy-dom`. `bun run test:watch` for watch mode. (Note: `bun test` invokes Bun's built-in test runner and does **not** run vitest — always go through `bun run`.)
- `@testing-library/jest-dom` matchers are registered globally by `src/test/setup.ts`, so `expect(node).toBeInTheDocument()` works without per-file imports.
- Render via `renderWithProviders` from `src/test/renderWithProviders.tsx` when the component touches `@tanstack/react-router` or `@tanstack/react-query`. It wraps a fresh `QueryClient` plus an in-memory `createMemoryHistory` router; use plain `render()` from `@testing-library/react` for everything else.
- Mock at the hook boundary with `vi.mock('@/hooks/...')` rather than wrapping the full provider stack — wagmi / Web3 / events providers are intentionally not wired in tests. See `src/components/blockpot/entries/EntryOptions/EntryCost/EntryCost.test.tsx` for the reference pattern and `src/test/README.md` for the full convention.
- Storybook stories `*.story.tsx` remain co-located with components as visual references; they have no executable assertions.

## Historical note

An earlier Mantine-based component tree at `/src/components/block-pot/` was removed during the migration to the current `/src/components/blockpot/` tree. Nothing remains to migrate.
